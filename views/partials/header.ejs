<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Egg Server Manager</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="text-light">
    <div class="background-animate"></div>
    
    <% if (typeof user !== 'undefined' && user) { %>
        <div class="d-flex" id="wrapper">
            <!-- Sidebar -->
            <%- include('sidebar') %>
            
            <!-- Page Content -->
            <div id="page-content-wrapper" class="w-100 d-flex flex-column" style="min-height: 100vh;">
                <nav class="navbar navbar-expand-lg navbar-dark glass-nav border-bottom-0 d-md-none mb-3">
                    <div class="container-fluid">
                        <button class="btn btn-link text-white" id="menu-toggle"><i class="fas fa-bars fa-lg"></i></button>
                        <span class="navbar-brand ms-2 fw-bold">Egg Manager</span>
                    </div>
                </nav>
                
                <div class="container-fluid p-4 flex-grow-1">
    <% } else { %>
        <div class="container">
    <% } %>
